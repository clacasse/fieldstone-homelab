---
- name: Provision GPU workstation
  hosts: gpu
  become: true
  vars:
    argocd_namespace: argocd
    # Override with: ansible-playbook ... -e timezone=America/Los_Angeles
    # timezone: UTC
  roles:
    - base
    - nvidia
    - k3s
    - argocd
