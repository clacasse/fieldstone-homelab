apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik-tls
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/clacasse/fieldstone-homelab
    targetRevision: HEAD
    path: clusters/default/apps/traefik-tls
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
